{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "New project",
  "architectural_notes": [],
  "known_issues": [
    "Server error/unavailable when loading Delivery History and Shipments (\"Failed to load... Please try again or contact support\")."
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-update-the-client-shipments-delivery-history-ui-error-handling-to-display-the-real-error-message-returned-by-react-query-when-available-instead-of-always-showing-only-the-generic-fallback-text-while-keeping-all-user-facing-text-in-english",
      "title": "Update the Client Shipments/Delivery History UI error handling to display the real error message returned by React Query (when available) instead of always showing only the generic fallback text, while keeping all user-facing text in English.",
      "reqIds": [
        "REQ-3"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Implement the missing backend (Motoko) admin session APIs used by the frontend so Admin Login no longer fails with “Service unavailable. Please contact support.” when the app attempts to call `actor.adminLogin(...)` and validate tokens via `actor.getAllClients(...)`.",
      "reqIds": [
        "REQ-1"
      ],
      "status": "pending"
    },
    {
      "id": "AR-2",
      "summary": "Implement the missing backend (Motoko) shipment/history APIs referenced by the frontend so the Client Dashboard can load shipments and delivery history without failing (including `getClientShipmentsBySessionToken(sessionToken)` and any other shipment methods that the frontend calls after login).",
      "reqIds": [
        "REQ-2"
      ],
      "status": "pending"
    }
  ],
  "projectName": "Fix service-unavailable errors for Admin Login and Client Shipments/Delivery History loading",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}